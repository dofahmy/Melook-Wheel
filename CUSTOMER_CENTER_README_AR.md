# مركز إدارة العملاء — وفر كاش

تمت إضافة تبويب **إدارة العملاء** داخل `/admin` مع الوظائف التالية:

- البحث والتصفية حسب البرنامج، حالة Telegram، تاريخ التسجيل، والعملاء الذين لم تصلهم رسالة الفحص.
- ترتيب العملاء من عناوين الأعمدة، مع جعل Online هو الترتيب الافتراضي ثم الأحدث نشاطًا.
- العميل الذي لا يملك نشاطًا مسجلًا يظهر Offline افتراضيًا، ويظهر Online عند نشاطه خلال آخر 5 دقائق.
- خيار `Online Now` هو الاختيار الافتراضي في قائمة فترة العملاء.
- قائمتا العملاء وإدارة العملاء مقسمتان إلى صفحات، 50 عميلًا في الصفحة، مع توسيع لوحة الإدارة والجداول.
- ترتيب الأعمدة يتم في قاعدة البيانات على جميع العملاء أولًا، ثم تُطبق الصفحات؛ لذلك الصفحة الثانية تكمل نفس الترتيب العام ولا تعيد ترتيب نفسها منفصلة.
- زر رسالة الترحيب يفتح عجلة الاختيار داخل Telegram كـ Mini App بدل فتحها في المتصفح الخارجي.
- عند وجود اسم للزر وترك خانة الرابط فارغة، يستخدم الإرسال للمحددين أيضًا `WHEEL_URL` تلقائيًا ويفتحه داخل Telegram.
- زر العجلة في رسائل العملاء يُرسل كـ Reply Keyboard Web App، وليس Inline، حتى تصل نتيجة `sendData` للبوت وتبدأ أسئلة العروض بعد توقف العجلة.
- أول دورة مكتملة لكل عميل تسجل جائزة ثابتة 2 جنيه (`EGYPT_FIRST_ROUND_CUSTOMER_REWARD=2.00`). كل الدورات التالية تُحسب من متغيرات ونِسَب EPC فقط، بدون أي حد أدنى أو تثبيت عند 0.40 جنيه.
- اختيار عميل واحد، مجموعة محددة، أو كل العملاء المطابقين للفلاتر.
- رسائل فردية وجماعية مع زر ورابط، ومتابعة نتائج الحملة.
- رسالة فحص العملاء القدامى بالنص المعتمد وزر «لف العجلة الآن».
- استبعاد العملاء المعروف أنهم حاظرون للبوت من الحملات ومن عدد المستلمين.
- اكتشاف الحظر وفك الحظر لحظيًا عبر `my_chat_member`، والتأكد مرة أخرى عند محاولة الإرسال.
- إضافة أو خصم الرصيد فرديًا أو جماعيًا مع دفتر حركة ومفتاح يمنع تكرار العملية.
- إعادة تنشيط عميل واحد أو مجموعة.
- سجل حملات، نتائج كل مستلم، وسجل تدقيق لإجراءات الأدمن.

## الملفات المعدلة

- `database.py`
- `admin_web.py`
- `admin_dashboard.html`
- `handlers.py`
- `main.py`

## التشغيل

لا توجد أوامر Migration منفصلة. عند تشغيل البوت، `database.init_db()` ينشئ الجداول والأعمدة الجديدة تلقائيًا بدون حذف البيانات القديمة.

ارفع الملفات الخمسة مكان الملفات المقابلة في GitHub ثم أعد تشغيل خدمة Railway.

## رسالة فحص العملاء القدامى

```text
أهلًا {first_name} 👋💚

وحشتنا في وفر كاش 🤖
رجعنا لك بعروض أكتر وهدايا أكبر، وفرصة تلف عجلة الهدايا وتكسب بطاقات هدايا فورية 🎁🎡

اضغط على الزر وابدأ دلوقتي 👇
```

يستخدم الزر `WHEEL_URL` من إعدادات المشروع إذا تُرك حقل الرابط فارغًا في لوحة الإدارة.
